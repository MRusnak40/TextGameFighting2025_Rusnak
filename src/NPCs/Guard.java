package NPCs;

public class Guard extends NPC{


    public Guard(NPCEnum type, String name, String dialog, String welcomeText, boolean isSpoken, boolean isImportant) {
        super(type, name, dialog, welcomeText, isSpoken, isImportant);
    }
}

package NPCs;

public class Villiger extends NPC{
    public Villiger(String name, String dialog, String welcomeText, boolean isSpoken, boolean isImportant) {
        super(name, dialog, welcomeText, isSpoken, isImportant);
    }
}

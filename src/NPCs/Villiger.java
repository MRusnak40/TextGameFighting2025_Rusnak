package NPCs;

public class Villiger extends NPC{

    public Villiger(NPCEnum type, String name, String dialog, String welcomeText, boolean isSpoken, boolean isImportant) {
        super(type, name, dialog, welcomeText, isSpoken, isImportant);
    }
}
